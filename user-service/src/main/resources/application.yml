server:
  port: 0

spring:
  datasource:
    url: jdbc:postgresql://localhost:55322/users_db?currentSchema=users_data
    username: userdata
    password: userdata
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  application:
    name: user-service
  liquibase:
    change-log: classpath:/liquibase/db.changelog.yml
    enabled: true
    default-schema: users_data

security:
  jwt:
    secret: eWdha2pyaGlnaUtKSkhJT1VIa2pib2xiNDU2Mjg1Nm5ianlna2pLSk5LSktKTkhOamtuamIsbW0uO3A7Ow==
    access: 3600000
    refresh: 2592000000

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    instance-id: ${spring.application.name}:${random.uuid}

aspect:
  timer:
    enabled: true
    log-level: warn
